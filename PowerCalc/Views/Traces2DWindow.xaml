<Window x:Class="TAlex.PowerCalc.Views.Traces2DWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:talex="http://schemas.talex-soft.com/2010/xaml/presentation"
        xmlns:behaviours="clr-namespace:TAlex.PowerCalc.Behaviours"
        xmlns:convs="clr-namespace:TAlex.PowerCalc.Converters"
        DataContext="{Binding Source={StaticResource viewModelLocator}, Path=Plot2DViewModel}"
        Background="{DynamicResource GradBrush}"
        Title="{Binding State.WindowCaption}" Height="300" Width="600" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        FocusManager.FocusedElement="{Binding ElementName=traceTextBox}">
    <Window.Resources>
        <talex:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <convs:InverseBooleanConverter x:Key="InverseBooleanConverter" />
    </Window.Resources>

    <Window.Style>
        <Style>
            <Style.Triggers>
                <DataTrigger Binding="{Binding CloseSignal}" Value="true">
                    <Setter Property="behaviours:WindowCloseBehavior.Close" Value="true" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Style>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="35" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <Expander Header="Traces" Grid.Column="1" DockPanel.Dock="Left" ExpandDirection="Left" Margin="5,0,0,0"
                      Visibility="{Binding State.ShowTracesList, Converter={StaticResource BooleanToVisibilityConverter}}" IsExpanded="True">               
                
                <ListView Name="TracesList" Width="150" SelectedIndex="0" ItemsSource="{Binding Traces}">
                </ListView>
            </Expander>

            <GroupBox Header="Settings" Padding="15" DataContext="{Binding Path=SelectedItem, ElementName=TracesList}" Background="{DynamicResource prefTabControlBgBrush}">
                <Grid>
                    <Grid.Resources>
                        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="VerticalAlignment" Value="Center" />
                        </Style>

                        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="Height" Value="23" />
                        </Style>

                        <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource {x:Type CheckBox}}">
                            <Setter Property="VerticalAlignment" Value="Center" />
                        </Style>
                    </Grid.Resources>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>

                    <TextBlock Text="f(x):" />
                    <TextBox Name="traceTextBox" Grid.Column="1" Text="{Binding Expression, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch" />

                    <TextBlock Grid.Row="1" Text="Minimum:" />
                    <TextBox Grid.Row="1" Grid.Column="1" Width="80" HorizontalAlignment="Left" Text="{Binding LowerBound, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding Path=IsChecked, ElementName=MinimumInfinity, Converter={StaticResource InverseBooleanConverter}, UpdateSourceTrigger=PropertyChanged}" />
                    <CheckBox Name="MinimumInfinity" Content="Negative Infinity" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" Margin="100,0,0,0" IsChecked="{Binding IsLowerBoundInfinity}" />
                    
                    <TextBlock Grid.Row="2" Text="Maxymum:" />
                    <TextBox Grid.Row="2" Grid.Column="1" Width="80" HorizontalAlignment="Left" Text="{Binding UpperBound, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding Path=IsChecked, ElementName=MaximumInfinity, Converter={StaticResource InverseBooleanConverter}, UpdateSourceTrigger=PropertyChanged}" />
                    <CheckBox Name="MaximumInfinity" Content="Positive Infinity" Grid.Row="2" Grid.Column="1" HorizontalAlignment="Left" Margin="100,0,0,0" IsChecked="{Binding IsUpperBoundInfinity}" />

                    <CheckBox Grid.Row="3" Content="Display" IsChecked="{Binding Display}" />

                    <talex:ColorChip Grid.Row="4" Content="Foreground" Margin="-2,0,0,0" />
                </Grid>
            </GroupBox>
        </DockPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Height="25">
            <Button MinWidth="80" Margin="0,0,8,0" Content="{Binding State.AcceptButtonCaption}" Command="{Binding AcceptCommand}" />
            <Button MinWidth="80" Content="_Cancel" IsCancel="True" />
        </StackPanel>
        
    </Grid>
</Window>
